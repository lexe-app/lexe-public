[package]
name = "app-rs"
version = "0.1.0"
authors = [
    "Max Fang <protonmail.com@maxfa.ng>",
    "Philip Hayes <philiphayes9@gmail.com>"
]
license = "PolyForm Noncommercial License 1.0.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
crate-type = [
    # Android requires dynamically linked native libs
    "cdylib",
]

[dependencies]
flutter_rust_bridge = { version = "1", default-features = false, features = [] }

[dev-dependencies]

[build-dependencies]
# env_logger = { version = "0", default-features = false }
flutter_rust_bridge_codegen = { version = "1", default-features = false, features = [] }
