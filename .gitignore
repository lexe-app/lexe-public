# Cargo-generated compiled files and executables
debug/
target/

# Explicitly ignore .git so we can use `--hidden` with tools like rg / fastmod
.git/

# Backup files generated by rustfmt
**/*.rs.bk

# ignore `nix build` output symlink
/result

# A place to put logs that you don't want fzf-ripgrep or cargo watch to pick up,
# useful when piping stdout to log files during debugging.
log/

# workspace-local vim/nvim configuration
.vim/

# Misc
.DS_Store
Session.vim
.aider*
